# Backend dependencies — VoiceAI (Python + FastAPI)
# Install into your venv with:  pip install -r requirements.txt
fastapi==0.141.1
uvicorn[standard]==0.52.4
google-genai==2.20.0
python-dotenv==1.2.3
